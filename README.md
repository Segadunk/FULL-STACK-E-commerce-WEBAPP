# -FULL-STACK-E-commerce-WEBAPP-
First full stack web application. A functional replica of web site where users can buy electronic goods
TIL; We need a start script in package.json because it is a standard convention, easy to just type "npm start" and also deployment platforms require or sniff around for npm start the same way browsers sniff for index.html when loading the homepage of a website